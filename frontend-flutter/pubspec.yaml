# SPDX-License-Identifier: AGPL-3.0
# Copyright (C) 2024-2026 Breakdown RS Contributors
# Co-authored-by: hy3 (opencode-go)
# Co-authored-by: qwen3.8-flash (opencode-go)


name: frontend_flutter
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  # PKCE S256 code challenge (lib/auth/pkce.dart) — pure Dart, no Flutter deps.
  crypto: ^3.0.0
  dio: ^5.11.0
  drift: ^2.34.3
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  flutter_secure_storage: ^11.0.0
  fpdart: ^1.2.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  riverpod_annotation: ^4.0.6

  # Generated OpenAPI client (breakdown_api), generated by scripts/regen-client.sh
  # from backend/openapi.yaml. Dio-based (`dart-dio` generator). Consumed as a
  # path dependency; its runtime deps (one_of, ...) are resolved transitively
  # from its own pubspec. built_value/built_collection are direct deps because
  # hand-written repositories (lib/data/) reference built_value types (e.g.
  # JsonObject) directly.
  #
  # The package lives in `vendor/breakdown_api/`, deliberately OUTSIDE the
  # Flutter root's own `lib/` tree: nesting a second Dart package under the
  # root's `packageUri: lib/` makes the kernel compiler (CFE) derive a different
  # language version for the package's generated `.g.dart` parts than for its
  # library, which fails to compile ("The language version override has to be
  # the same in the library and its part(s)"). The package name and import path
  # (`package:breakdown_api/...`) are unaffected.
  breakdown_api:
    path: vendor/breakdown_api
  built_value: ^8.9.0
  built_collection: ^5.1.1

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
dev_dependencies:
  flutter_test:
    sdk: flutter

  # On-device E2E smoke tests (AGENTS.md §6 Tier 4): run via
  # `dart run integration_test run-tests` (manifest mode) against a device/emulator.
  integration_test:
    sdk: flutter

  # `flutter_gherkin` drives the critical acceptance scenarios on device (see
  # `features-spec/` + `integration_test/gherkin/`, AGENTS.md §6 / spec
  # flutter-gherkin-hybrid). It shells out to `flutter drive`, which requires
  # the instrumented app entrypoint to call `enableFlutterDriverExtension()`,
  # so `flutter_driver` is a direct dev dependency here.
  flutter_driver:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.8
  freezed: ^4.0.1
  build_runner: ^2.16.0
  json_serializable: ^6.14.1
  drift_dev: ^2.34.5
  flutter_gherkin: ^2.0.0
  # Direct dependency: the step/world/hook files import gherkin symbols
  # (StepDefinitionGeneric, Hook, Tag, TestConfiguration, StepContext) from
  # `package:gherkin/gherkin.dart`. flutter_gherkin depends on it transitively
  # but does not re-export it, so it is declared directly to satisfy the
  # analyzer's depend_on_referenced_packages rule.
  gherkin: ^2.0.2
  flutter_secure_storage_platform_interface: ^2.0.3
  test: ^1.27.1
  breakdown_lints:
    path: packages/breakdown_lints

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # The integration_test v2 tooling (`dart run integration_test run-tests`,
  # Tier-4 E2E smoke tests, AGENTS.md §6) is enabled by the
  # `integration_test` dev_dependency below — no `flutter: config:` flag is
  # needed or supported.

  # Per-flavor pinned CA bundles for Dio TLS pinning (AGENTS.md §5 / ADR-024 /
  # issue #301). The client trusts ONLY the CA in the active flavor's bundle
  # (`buildApiClient` in lib/src/network/api_client.dart), never the system
  # trust store. Replace the prod placeholder with the real edge CA before
  # shipping.
  assets:
    - assets/certs/dev/ca.pem
    - assets/certs/prod/ca.pem

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
